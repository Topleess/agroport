## Deploy secrets

Create these GitHub Actions secrets:

- `DEPLOY_HOST`
- `DEPLOY_PORT` (optional, defaults to `22`)
- `DEPLOY_USER`
- `DEPLOY_PATH` (optional, defaults to `/opt/apps/agroport`)
- `DEPLOY_SSH_KEY`
