services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: agroport
      POSTGRES_PASSWORD: agroport
      POSTGRES_DB: agroport
    ports:
      - "5432:5432"
    volumes:
      - agroport_postgres_data:/var/lib/postgresql/data

volumes:
  agroport_postgres_data:
